const validUserName = 'student';
const validPassword = 'Password123';

const invalidUserName = 'incorrectUser';
const incorrectPassword = 'incorrectPassword';

const validUserNameWithSpecialChar = '\\student\$\@'

const loggedInSuccessfullyUrl = 'logged-in-successfully';
const loggedInSuccessfullyMsg = 'Logged In Successfully';

const invalidUsernameErrorMsg = 'Your username is invalid!';
const invalidPasswordErrorMsg = 'Your password is invalid!';

 const LoginTestData = {
    validUserName,
    validPassword,
    loggedInSuccessfullyUrl,
    loggedInSuccessfullyMsg,
    invalidUserName,
    invalidUsernameErrorMsg,
    invalidPasswordErrorMsg,
    incorrectPassword,
    validUserNameWithSpecialChar
}
export default LoginTestData;